// JS declarate function(expression way)
let log = function(message){
    console.log(message);
}

// TS declarate function(arrow function)
let doLog = (message) => {
    console.log(message)
}
//Exclude braces if have only 1 line
let doLog2 = (message) => console.log(message);
