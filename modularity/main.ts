//Import type to use in main.ts
//These types can be: functions, classes, simple variables or objects
import {Point} from './point' // {type1,type2,type3}

//Instance
let point = new Point(1,2); 
point.draw(); 
